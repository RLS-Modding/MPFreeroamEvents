# MPFreeroamEvents Work — 2026-02-06

## Refactor Branch Created
- Branch: `refactor` from `main`
- All work today on this branch

## Commits (in order)
1. `f9323b3` — Major codebase refactor: extracted helpers, improved nil-safety, DRY cleanup
   - Split `onBeamNGTrigger` into handler functions (handleStaging, handleStart, handleCheckpoint, handleFinish, handlePits)
   - Extracted shared payout logic from payoutRace/payoutDragRace
   - Added career helpers: isCareerActive(), getInventoryIdSafe()
   - Constants: SPEED_UNIT_MPS_TO_MPH, NEW_BEST_BONUS, IN_RANGE_BONUS, XP_DIVISOR, MAX_REWARD_MULTIPLIER
   - Fixed nil-safety: getRoadNodes, exitRace, displayMessage signature
   - Removed duplicate getRaceLabel

2. `36290d4` — Editor overhaul: alt routes, validation, reward graph, UI polish
   - Alt route editing (the main TODO)
   - Event duplication, search/filter
   - Reward curve graph with PlotLines1
   - Road path visualization (green=main, blue=alt) via debugDrawer
   - Missing fields: top speed goal, min checkpoint distance, drift target time
   - Save validation modal, teleport to start
   - Help popups for all non-obvious fields

3. `0245cee` — Track distance system
   - processRoad: calculateTrackDistances() + calculateLiveDistance()
   - Checkpoint messages show "Checkpoint 3/8 — 890m / 3.45km"
   - Editor: track length, per-checkpoint distances, gap analysis with warnings
   - utils: formatDistance() helper

4. `9b69e9a` — Fixed PlotLines1: use TableToArrayFloat (not ArrayFloat or ffi cdata)
5. `1fbb38a` — Improved reward graph: dual curves for damage events, reward cap, no sci notation

## BeamNGImGui Skill Created
- Location: `~/projects/Beam-skills/.cursor/skills/BeamNGImGui/`
- 6 reference docs: api-reference, patterns, cheat-sheet, data-types, editor-windows, flags-and-constants
- Key learning: PlotLines1 requires `im.TableToArrayFloat()` + `im.GetLengthArrayFloat()`, NOT raw tables or ffi cdata

## Design Proposals Written
- `~/projects/MPFreeroamEvents/DESIGN-PROPOSALS.md`
- Route Graph System: segments + layouts replacing alt routes, direction-aware checkpoint validation for overlaps
- Track Distance System: implemented ✅
- Direction-aware checkpoints handle ALL overlaps generically (dot product of velocity vs expected direction)
- Fully backward compatible — existing race_data.json unchanged

## Key Decisions
- All overlap handling (figure-8, crossings, shared roads) via generic direction check — no special-casing per track shape
- Layouts are additive to existing format — old events work unchanged
- Editor improvements use actual utils functions for reward calculations (not hardcoded math)
- Reward cap: baseReward * 30 (MAX_REWARD_MULTIPLIER)

## Next Steps (discussed with Tristan)
- Implement direction-aware checkpoints in runtime
- Implement layouts/segments system in runtime
- Update editor to support layouts/segments
- Tristan confirmed: must be backward compatible, no map rework needed
